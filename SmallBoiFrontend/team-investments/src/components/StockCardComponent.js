import React from "react";
import useStyles from "./StockCardComponentStyle";
export default function StockCardComponent(props){
    var classes = useStyles();
    return (
        <div className={classes.StockCardBorder}>
test
        </div>
    )
}